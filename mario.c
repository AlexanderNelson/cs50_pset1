#include <stdio.h>
#include <cs50.h>

int main()
{
    int lines, height, spaces;
    
    
    /* prompt user for height */
    {
    do
        printf ("Hey Grand Architect! How high of a pyramid would please you? ");
        height = getInt ();
    }
    while (height < 1 || height > 23) /* prompt again if invalid entry */
    
    for (lines = 0, lines < height, lines++);
    {
        //print spaces
        spaces = height - 1;
        spaces--;
        printf(" ");
        
        //print hashes
        //print new lines
    }
    //code outside
        
 
}